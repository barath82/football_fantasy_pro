# Copy to .env and adjust if your API runs on a different port.
VITE_API_URL=http://localhost:3001

# PostHog analytics — from your project settings at posthog.com. Leave unset
# to disable analytics entirely (e.g. local dev).
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=https://us.i.posthog.com
